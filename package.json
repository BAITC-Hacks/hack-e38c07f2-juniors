{
  "name": "smartflow",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SmartFlow — платформа практических задач для бизнеса и студенческих команд",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.9"
  }
}
